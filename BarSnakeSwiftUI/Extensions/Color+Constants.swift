import SwiftUI

extension Color {
    static let gameOverBackground = Color.red.opacity(Constants.strongColorOpacity)
    static let primary = Color(Constants.primaryColor)
    static let secondary = Color(Constants.secondaryColor)
    static let defaultFontColor = Color(Constants.fontColor)
}
